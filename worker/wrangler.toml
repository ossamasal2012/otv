name = "yallagoal-channels-proxy"
main = "yallagoal-channels-worker.js"
compatibility_date = "2026-07-15"

[vars]
GITHUB_OWNER = "ossamasal2012"
GITHUB_REPO = "yalla-data"
GITHUB_REF = "main"
GITHUB_CHANNELS_PATH = "channels.json"

# Required secrets (set outside git):
# wrangler secret put GITHUB_TOKEN
