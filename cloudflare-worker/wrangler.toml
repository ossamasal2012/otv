name = "yallagoal-active-users"
main = "worker.js"
compatibility_date = "2026-06-01"

# ربط اسم "PRESENCE_ROOM" بكلاس PresenceRoom المُصدَّر من worker.js
[[durable_objects.bindings]]
name = "PRESENCE_ROOM"
class_name = "PresenceRoom"

# يسجّل الكلاس كـ Durable Object جديد بتخزين SQLite (مطلوب مرة واحدة فقط عند أول نشر)
[[migrations]]
tag = "v1"
new_sqlite_classes = ["PresenceRoom"]
